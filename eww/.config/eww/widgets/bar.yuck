(defwidget bar []
  (centerbox
    :class "bar"
    :orientation "v"

    (box
      :class "bar-top"
      :orientation "v"
      :valign "start"
      :space-evenly false
      :spacing 8
      (system-power)
      (workspaces))

    (box
      :class "bar-center"
      :orientation "v"
      :valign "center"
      :space-evenly false)

    (box
      :class "bar-bottom"
      :orientation "v"
      :valign "end"
      :space-evenly false
      :spacing 6
      
      (volume)
      (network)
      (bluetooth)
      (vpn)
      (keyboard-layout)
      (battery)
      (clock))))

(defwindow sidebar
  :monitor 0
  :geometry
    (geometry
      :x "4px"
      :y "0px"
      :width "56px"
      :height "90%"
      :anchor "right center")
  :exclusive true
  :stacking "fg"
  :focusable false

  :namespace "eww-sidebar"
  
  (bar))
